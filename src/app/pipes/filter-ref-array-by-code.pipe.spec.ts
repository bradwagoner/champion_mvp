import { FindRefArrayByCodePipe } from './filter-ref-array-by-code.pipe';

describe('FilterRefArrayByCodePipe', () => {
  it('create an instance', () => {
    const pipe = new FindRefArrayByCodePipe();
    expect(pipe).toBeTruthy();
  });
});
