import { JointMeasurementToDegreePipe } from './joint-measurement-to-degree.pipe';

describe('JointMeasuirementToDegreePipe', () => {
  it('create an instance', () => {
    const pipe = new JointMeasurementToDegreePipe();
    expect(pipe).toBeTruthy();
  });
});
