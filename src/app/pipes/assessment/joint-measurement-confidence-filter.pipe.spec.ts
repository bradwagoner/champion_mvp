import { JointMeasurementConfidenceFilterPipe } from './joint-measurement-confidence-filter.pipe';

describe('JointMeasurementConfidenceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new JointMeasurementConfidenceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
