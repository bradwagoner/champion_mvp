import { FilterAssessmentsByJointPipe } from './filter-assessments-by-joint.pipe';

describe('FilterAssessmentsByJointPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAssessmentsByJointPipe();
    expect(pipe).toBeTruthy();
  });
});
