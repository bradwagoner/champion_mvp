import { UserProfileCompletePipe } from './user-profile-complete.pipe';

describe('UserProfileCompletePipe', () => {
  it('create an instance', () => {
    const pipe = new UserProfileCompletePipe();
    expect(pipe).toBeTruthy();
  });
});
